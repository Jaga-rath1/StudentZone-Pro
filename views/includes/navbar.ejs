<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm sticky-top">
    <div class="container">

        <!-- Logo -->
        <a class="navbar-brand fw-bold fs-3" href="/users">
            🎓 StudentZone
        </a>

        <!-- Mobile Toggle -->
        <button class="navbar-toggler border-0" type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar Links -->
        <div class="collapse navbar-collapse" id="navbarNav">

            <ul class="navbar-nav mx-auto gap-lg-2">

                <li class="nav-item">
                    <a class="nav-link active px-3 rounded-pill" href="/dashboard">
                        Dashboard
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link px-3 rounded-pill" href="/users">
                        Students
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link px-3 rounded-pill" href="/courses">
                        Courses
                    </a>
                </li>


            </ul>

            <!-- Right Side Buttons -->
            <div class="d-flex gap-2">
                <% if(!currUser){ %>
                <a href="/login" class="btn btn-outline-light rounded-pill">
                    Login
                </a>

                <a href="/signup" class="btn btn-warning rounded-pill fw-semibold">
                    Signup
                </a>
                <% } %>
                <% if(currUser){ %>

                <a href="/logout" class="btn btn-warning rounded-pill fw-semibold">
                    Logout
                </a>
                <% } %>

            </div>

        </div>

    </div>
</nav>

<style>
.navbar{
    backdrop-filter: blur(10px);
}

.nav-link{
    transition: all 0.3s ease;
}

.nav-link:hover{
    background-color: rgba(255,255,255,0.15);
    transform: translateY(-2px);
}

.navbar-brand{
    letter-spacing: 1px;
}

.btn{
    transition: all 0.3s ease;
}

.btn:hover{
    transform: translateY(-2px);
}
</style>